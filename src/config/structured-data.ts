/**
 * JSON-LD structured data — deferred until credentials are verified (design §15).
 *
 * TODO(SPEC-future): emit Person + ProfessionalService schema when
 * professional credentials and service details are confirmed with the client.
 */

// export const structuredData = null;
